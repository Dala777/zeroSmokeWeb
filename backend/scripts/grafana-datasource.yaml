apiVersion: 1

datasources:
  - name: ZeroSmoke API
    type: yesoreyeram-infinity-datasource
    access: proxy
    url: http://localhost:5000/api/admin/stats
    isDefault: false
    editable: true
    uid: ZS_API
    jsonData:
      auth_method: "bearer"
    secureJsonData:
      bearerToken: "YOUR_ADMIN_JWT_TOKEN"
